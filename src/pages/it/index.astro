---
import Layout from '../../layouts/Layout.astro';
import { getTranslations } from '../../i18n/utils';

const t = getTranslations('it');
---

<Layout>
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="container hero-content">
      <img src="/images/logo.png" alt="Maria Golia Concorso Internazionale di Pianoforte" class="hero-logo" />

      <p class="hero-subtitle">CONCORSO INTERNAZIONALE DI PIANOFORTE</p>

      <h1 class="hero-date">1-3 Maggio 2026</h1>
      <p class="hero-location">Castello Ducale di Agliè, Italia</p>

      <a href="/it/iscrizione" class="btn hero-cta">Iscriviti Ora</a>
    </div>
  </section>

  <section class="section welcome">
    <div class="container">
      <h2 class="welcome-title">Benvenuti al Concorso Internazionale di Pianoforte <em>Maria Golia</em></h2>
      <p class="welcome-text">
        Un prestigioso concorso pianistico nelle magnifiche sale barocche del Castello Ducale di Agliè in Piemonte.
        Giovani pianisti da tutto il mondo competono per premi e l'opportunità di esibirsi in rinomate sale da concerto.
      </p>
    </div>
  </section>

  <section class="section categories">
    <div class="container">
      <div class="cards">
        <div class="card">
          <h3>Sezione I: Categorie Giovanili</h3>
          <p>Quattro categorie d'età per giovani pianisti fino a 18 anni. Un ambiente stimolante per far emergere nuovi talenti.</p>
          <a href="/it/concorso#youth">Scopri di più →</a>
        </div>
        <div class="card">
          <h3>Sezione II: Internazionale</h3>
          <p>Aperta a pianisti dai 18 ai 32 anni. Preselezione seguita da finale con premi in denaro e opportunità concertistiche.</p>
          <a href="/it/concorso#international">Scopri di più →</a>
        </div>
      </div>
    </div>
  </section>

  <section class="section venue">
    <div class="container">
      <h2 class="section-title">La Location</h2>
      <div class="venue-content">
        <h3>Castello Ducale di Agliè</h3>
        <p>
          Una magnifica residenza barocca in Piemonte. Le storiche sale del castello offrono un contesto eccezionale
          per il concorso, unendo patrimonio culturale ed eccellenza musicale.
        </p>
        <p class="venue-address">Piazza Castello 2, 10011 Agliè (TO), Italia</p>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: flex-start;
    justify-content: center;
    text-align: center;
    overflow: hidden;
    padding: 5.5rem 0 5rem;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg,
        rgba(248, 246, 243, 0.5) 0%,
        rgba(248, 246, 243, 0.7) 50%,
        rgba(248, 246, 243, 0.9) 100%
      ),
      url('/images/hero-bg.png');
    background-color: #f5f2ef;
    background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    padding: 0 1.5rem;
  }

  .hero-logo {
    max-width: 750px;
    width: 100%;
    height: auto;
    margin-bottom: 0;
  }

  .hero-subtitle {
    font-family: var(--font-serif);
    font-size: 0.9375rem;
    letter-spacing: 0.2rem;
    color: var(--color-dark);
    margin-bottom: 0.5rem;
    font-weight: 500;
  }

  .hero-date {
    font-family: var(--font-serif);
    font-size: 2.25rem;
    font-weight: 500;
    color: var(--color-dark);
    margin-bottom: 0.125rem;
  }

  .hero-location {
    font-size: 1rem;
    color: var(--color-text);
    margin-bottom: 1rem;
  }

  .hero-cta {
    font-size: 1.25rem;
    padding: 1.1rem 3rem;
    min-width: 16rem;
    background: var(--color-button);
  }

  .hero-cta:hover {
    background: #4d6a7e;
  }

  /* Welcome section */
  .welcome {
    background: var(--color-light);
    border-top: 1px solid var(--color-border);
  }

  .welcome-title {
    font-size: 1.75rem;
    font-weight: 500;
    color: var(--color-dark);
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .welcome-title em {
    font-style: italic;
  }

  .welcome-text {
    max-width: 700px;
    margin: 0 auto;
    text-align: center;
    color: var(--color-text);
    line-height: 1.8;
  }

  /* Categories cards */
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .card {
    background: var(--color-white);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid var(--color-border);
  }

  .card h3 {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    color: var(--color-dark);
  }

  .card p {
    color: var(--color-text);
    margin-bottom: 1.5rem;
    line-height: 1.7;
  }

  .card a {
    font-weight: 500;
  }

  /* Venue section */
  .venue {
    background: var(--color-white);
  }

  .venue-content {
    max-width: 700px;
  }

  .venue-content h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .venue-content p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: var(--color-text);
  }

  .venue-address {
    font-weight: 500;
    color: var(--color-dark) !important;
  }

  @media (max-width: 600px) {
    .hero {
      padding: 6rem 0 4.5rem;
    }

    .hero-logo {
      max-width: 600px;
    }

    .hero-date {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 0.95rem;
      letter-spacing: 0.15rem;
      margin-bottom: 1.75rem;
    }

    .hero-location {
      font-size: 1.125rem;
      margin-bottom: 2rem;
    }

    .hero-cta {
      font-size: 1.0625rem;
      min-width: 12rem;
      padding: 0.95rem 2.25rem;
    }
  }
</style>
